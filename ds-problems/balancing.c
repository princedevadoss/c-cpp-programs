#include <stdio.h>

int main() {
    char str[100];
    int stack[100], top = 0, n = 0;

    printf("Please enter the string\n");
    scanf("%s", &str);

    for(int i=0;str[i]!='\0';i++) {
        
    }

    printf("Length is %d\n", n);
    return 0;
}